model Formation {
  id           String  @id @default(uuid())
  slug         String  @unique
  title        String
  notionPageId String
  imageUrl     String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  Course    Course[]
}